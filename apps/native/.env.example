# Copy to .env.local and fill values (see Convex dashboard + Clerk).
EXPO_PUBLIC_CONVEX_URL=
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
# Same host as Convex deployment, path /privacy — use after `npx convex deploy`.
EXPO_PUBLIC_PRIVACY_POLICY_URL=
